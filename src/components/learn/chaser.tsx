import React from 'react';

import Image from 'next/image';

export default function ChaserInfo() {
  return (
    <div>
      <Image src={`/img/ratio.png`} width={1000} height={560} alt="ratio" />
    </div>
  );
}
